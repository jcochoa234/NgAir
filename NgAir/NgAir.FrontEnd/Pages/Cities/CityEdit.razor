@page "/cities/edit/{CityId:int}"
@inherits FeedbackComponent<int>

@if (City is null)
{
    <Loading />
}
else
{
    <FormWithName @ref="cityForm" Label="City:" Model="City" OnValidSubmit="SaveAsync" />
}