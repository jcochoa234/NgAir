@page "/countries/edit/{Id:int}"
@inherits FeedbackComponent<int>

@if(country is null)
{
    <Loading/>
}
else
{
    <FormWithName @ref="countryForm" Label="Country:" Model="country" OnValidSubmit="EditAsync" />
}