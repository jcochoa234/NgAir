@page "/states/edit/{StateId:int}"
@inherits FeedbackComponent<int>

@if (State is null)
{
    <Loading />
}
else
{
    <FormWithName @ref="stateForm" Label="State:" Model="State" OnValidSubmit="SaveAsync" />
}